<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CarbonTracer" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.CarbonTracer" parent="Base.Theme.CarbonTracer" />

    <!-- Style for circular ShapeableImageView -->
    <style name="ShapeAppearanceOverlay.App.CornerSize50Percent" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

    <!-- Custom TextAppearance for BottomNavigationView items -->
    <style name="BottomNavigationView.TextAppearance" parent="TextAppearance.AppCompat.Caption">
        <item name="android:textSize">10sp</item> <!-- Adjust size as needed -->
    </style>

</resources>